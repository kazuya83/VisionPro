import { Component } from "react";

class WebHistory extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>WebHistory</div>
        )
    }
}

export default WebHistory;